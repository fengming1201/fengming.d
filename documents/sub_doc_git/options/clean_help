usage: git clean [-d] [-f] [-i] [-n] [-q] [-e <pattern>] [-x | -X] [--] <paths>...

    -q, --quiet           不打印已删除文件的名称
    -n, --dry-run         试运行，不真正执行删除
    -f, --force           强制
    -i, --interactive     交互式删除,即删除前 y/n
    -d                    删除整个目录下
    -e, --exclude <pattern>  
                          添加<pattern>以忽略规则
    -x                    (小x)也删除忽略的文件
    -X                    (大X)仅删除忽略的文件

########################################################
例如：将本地还原到本地最新的提交状态，像刚刚clone一样
git clean -dfx

快速参考
场景                    命令                              说明
预览要删除的内容         git clean -n 或 git clean -nd     安全第一
只删文件，不删文件夹     git clean -f                      保留空目录
删除文件和文件夹         git clean -fd                     最常用
彻底清理所有            git clean -fdx                    包括.gitignore的文件
交互式删除              git clean -i                      逐个确认

提示:不清楚会删除哪些未跟踪文件，可以先用-n 选项试运行。