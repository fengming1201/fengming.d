set_tests_properties
--------------------

设置测试的属性。

 set_tests_properties(test1 [test2...] PROPERTIES prop1 value1 prop2 value2)

为测试设置属性。如果未找到测试，CMake将报告错误。
``Generator expressions`` 将展开为与测试的 ``add_test()`` 调用所支持的相同。

另请参见 ``set_property(TEST)`` 命令。

参见 :ref:`Test Properties` 了解CMake已知的属性列表。