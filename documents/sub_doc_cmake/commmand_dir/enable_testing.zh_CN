enable_testing
--------------

为当前目录及其子目录启用测试。

 enable_testing()

为此目录及其子目录启用测试。

此命令应位于源目录根目录中，
因为 ctest 期望在构建目录根目录中找到测试文件。

当包含 ``CTest`` 模块时，此命令会自动调用，
除非关闭了 ``BUILD_TESTING`` 选项。

另请参见 ``add_test()`` 命令。