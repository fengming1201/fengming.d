option
------

提供用户可以选择的选项。

 option(<variable> "<help_text>" [value])

为用户提供一个选项，可选择为 ``ON`` 或 ``OFF``。
如果未提供初始 ``<value>``，则使用 ``OFF``。
如果 ``<variable>`` 已经设置为普通变量或缓存变量，
则命令不执行任何操作（参见策略 ``CMP0077``）。

如果您有依赖于其他选项值的选项，请参见
``CMakeDependentOption`` 的模块帮助。