以下是一个使用SDCC编译51单片机程序的示例：

假设我们有一个名为 `led.c` 的C程序，用于控制51单片机上的LED灯，内容如下：

```c
#include <reg51.h>

#define LED_PIN P1

void delay(unsigned int ms) {
    unsigned int i, j;
    for(i = 0; i < ms; i++)
        for(j = 0; j < 114; j++);
}

void main() {
    LED_PIN = 0x00; // 初始时关闭LED灯

    while(1) {
        LED_PIN = 0xFF; // 打开LED灯
        delay(500); // 延时500毫秒

        LED_PIN = 0x00; // 关闭LED灯
        delay(500); // 延时500毫秒
    }
}
```

要使用SDCC编译这个程序为51单片机的目标代码，可以按照以下步骤进行：

1. 打开终端或命令提示符窗口。
2. 进入包含 `led.c` 文件的目录。

接下来，执行以下命令：

```shell
sdcc led.c
```

SDCC将会生成多个文件，包括目标文件 `led.rel`、中间文件 `led.ihx` 和其他一些辅助文件。

如果需要将程序下载到51单片机上运行，可以使用适当的下载工具，如 `stcgal` 或 `flashrom`，具体工具取决于您使用的51单片机型号。

请注意，上述示例中使用的是51单片机的特定寄存器和端口定义，适用于常见的51单片机开发板。
如果您使用的是不同型号的51单片机或自定义硬件，可能需要根据具体情况进行相应的修改。

此外，还可以使用SDCC的编译选项来进行更详细的配置，例如指定特定的单片机型号、优化级别等。具体的编译命令和选项可以根据您的需求和目标平台进行调整。