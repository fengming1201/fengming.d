
Usage:  docker manifest COMMAND

The **docker manifest** command has subcommands for managing image manifests and
manifest lists. A manifest list allows you to use one name to refer to the same image
built for multiple architectures.

To see help for a subcommand, use:

    docker manifest CMD --help

For full details on using docker manifest lists, see the registry v2 specification.

EXPERIMENTAL:
  docker manifest is an experimental feature.
  Experimental features provide early access to product functionality. These
  features may change between releases without warning, or can be removed from a
  future release. Learn more about experimental features in our documentation:
  https://docs.docker.com/go/experimental/

Commands:
  annotate    Add additional information to a local image manifest
  create      Create a local manifest list for annotating and pushing to a registry
  inspect     Display an image manifest, or manifest list
  push        Push a manifest list to a repository
  rm          Delete one or more manifest lists from local storage

Run 'docker manifest COMMAND --help' for more information on a command.
===============================================================================
**docker manifest**命令具有用于管理镜像清单和清单。清单列表允许您使用一个名称来引用同一镜像
为多种架构而构建。

To see help for a subcommand, use:

    docker manifest CMD --help

有关使用docker清单列表的完整详细信息，请参阅注册表v2规范。

实验性：
  docker清单是一个实验性功能。
  实验性功能提供了对产品功能的早期访问。这些功能可能会在没有警告的情况下在版本之间发生变化，或者可以从中删除
  未来的释放。在我们的文档中了解更多关于实验功能的信息：
  https://docs.docker.com/go/experimental/

Commands:
  annotate    向本地映像清单添加其他信息
  create      创建本地清单列表，用于注释和推送到仓库
  inspect     显示图像清单或清单列表
  push        将清单列表推送到存储库
  rm          从本地存储中删除一个或多个清单列表

Run 'docker manifest COMMAND --help' for more information on a command.