用法：  docker compose ls [选项]

列出正在运行的 compose 项目

选项：
  -a, --all             显示所有已停止的 Compose 项目
      --dry-run         以试运行模式执行命令
      --filter filter   根据给定条件过滤输出
      --format string   设置输出格式。可选值：[table | json]（默认 "table"）
  -q, --quiet           仅显示项目名称
