用法：  docker compose attach [选项] SERVICE

将本地标准输入、输出和错误流附加到服务的运行中容器

选项：
      --detach-keys string   覆盖从容器分离的按键序列
      --dry-run              以试运行模式执行命令
      --index int            若服务有多个副本，指定容器索引
      --no-stdin             不附加 STDIN
      --sig-proxy            将所有接收到的信号代理给进程（默认 true）
