用法:  docker compose rm [选项] [服务...]

删除已停止的服务容器

默认情况下，附加到容器的匿名卷不会被删除。您
可以使用 -v 覆盖此设置。要列出所有卷，请使用 "docker volume ls"。

不在卷中的任何数据都将丢失。

选项:
      --dry-run   在试运行模式下执行命令
  -f, --force     不要求确认删除
  -s, --stop      如果需要，在删除前停止容器
  -v, --volumes   删除附加到容器的任何匿名卷