用法：  docker compose rm [选项] [SERVICE...]

移除已停止的服务容器

默认情况下，附加到容器的匿名卷不会被移除。可使用 -v 覆盖此行为。
要列出所有卷，请使用 "docker volume ls"。

不在卷中的数据将会丢失。

选项：
      --dry-run   以试运行模式执行命令
  -f, --force     移除时不请求确认
  -s, --stop      必要时先停止容器再移除
  -v, --volumes   移除附加到容器的匿名卷
