用法：  docker compose exec [选项] SERVICE COMMAND [ARGS...]

在运行中的容器内执行命令

选项：
  -d, --detach            分离模式：在后台运行命令
      --dry-run           以试运行模式执行命令
  -e, --env stringArray   设置环境变量
      --index int         若服务有多个副本，指定容器索引
  -T, --no-tty            禁用伪 TTY 分配。默认情况下 'docker compose exec' 会分配 TTY（默认 true）
      --privileged        为进程授予扩展权限
  -u, --user string       以指定用户身份运行命令
  -w, --workdir string    此命令的工作目录路径
