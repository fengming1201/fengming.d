用法:  docker compose pull [选项] [服务...]

拉取服务镜像

选项:
      --dry-run                在试运行模式下执行命令
      --ignore-buildable       忽略可以构建的镜像
      --ignore-pull-failures   拉取可以拉取的镜像，忽略拉取失败的镜像
      --include-deps           同时拉取声明为依赖项的服务
      --policy string          应用拉取策略 ("missing"|"always")
  -q, --quiet                  拉取时不打印进度信息