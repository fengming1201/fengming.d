用法：  docker compose [选项] 命令

使用 Docker 定义并运行多容器应用

选项：
      --all-resources              包含所有资源，包括未被服务使用的资源
      --ansi string                控制何时输出 ANSI 控制字符（"never"|"always"|"auto"）（默认 "auto"）
      --compatibility              以向后兼容模式运行 compose
      --dry-run                    以试运行模式执行命令
      --env-file stringArray       指定替代的环境变量文件
  -f, --file stringArray           Compose 配置文件
      --parallel int               控制最大并行度，-1 表示无限制（默认 -1）
      --profile stringArray        指定要启用的 profile
      --progress string            设置进度输出类型（auto、tty、plain、json、quiet）
      --project-directory string   指定替代的工作目录
                                   （默认：第一个指定的 Compose 文件所在路径）
  -p, --project-name string        项目名称

管理命令：
  bridge                  将 compose 文件转换为另一种模型

命令：
  attach                  将本地标准输入、输出和错误流附加到服务的运行中容器
  build                   构建或重新构建服务
  commit                  根据服务容器的变更创建新镜像
  config                  解析、消解依赖并以规范格式渲染 compose 文件
  cp                      在服务容器与本地文件系统之间复制文件/文件夹
  create                  为服务创建容器
  down                    停止并移除容器、网络
  events                  接收来自容器的实时事件
  exec                    在运行中的容器内执行命令
  export                  将服务容器的文件系统导出为 tar 归档
  images                  列出已创建容器使用的镜像
  kill                    强制停止服务容器
  logs                    查看容器输出
  ls                      列出正在运行的 compose 项目
  pause                   暂停服务
  port                    打印端口绑定对应的公开端口
  ps                      列出容器
  publish                 发布 compose 应用
  pull                    拉取服务镜像
  push                    推送服务镜像
  restart                 重启服务容器
  rm                      移除已停止的服务容器
  run                     在服务上运行一次性命令
  scale                   扩展服务规模
  start                   启动服务
  stats                   显示容器资源使用情况的实时流
  stop                    停止服务
  top                     显示正在运行的进程
  unpause                 恢复已暂停的服务
  up                      创建并启动容器
  version                 显示 Docker Compose 版本信息
  volumes                 列出卷
  wait                    阻塞直到所有（或指定）服务的容器停止
  watch                   监视服务的构建上下文，文件更新时重新构建/刷新容器

运行 'docker compose 命令 --help' 可查看某个命令的更多信息。
