 ───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
  Claude Code v2.1.63  general   commands   custom-commands  (←/→ 或 tab 键切换)
                                                   ^
浏览自定义命令：

  ❯ /batch
    研究并规划大规模变更，然后在 5-30 个独立的工作树代理中并行执行，每个代理都会创建一个 PR。(捆绑)
    /debug
    通过读取会话调试日志来调试您当前的 Claude Code 会话。(捆绑)
    /simplify
    审查更改后的代码以检查可重用性、质量和效率，然后修复发现的任何问题。(捆绑)