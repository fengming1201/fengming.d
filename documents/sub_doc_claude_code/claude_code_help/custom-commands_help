  Help  General   Commands   Custom commands (←/→ 或 tab 键切换)
                                    ^
浏览自定义命令

    /batch                      -- 调研并规划大规模变更，随后通过 5–30 个独立 worktree Agent
                                    并行执行，各自打开 Pull Request
    /claude-api                 -- 构建、调试并优化 Claude API / Anthropic SDK 应用；
                                    使用此 skill 构建的应用应启用 prompt caching
    /code-review                -- 按指定 effort 级别（low/medium/high 等）审查当前 diff
                                    的正确性 bug，以及复用、简化与效率方面的改进
    /debug                      -- 为本会话启用调试日志并协助诊断问题
    /deep-research              -- 深度调研：扇出式网络搜索、抓取来源、对抗性验证主张，合成带引用的报告
    /fewer-permission-prompts   -- 扫描对话记录中常见的只读 Bash 与 MCP 工具调用，
                                    向项目 .claude/settings.json 添加优先级 allowlist
    /loop                       -- 按固定间隔重复运行提示词或斜杠命令（例如 /loop 5m /foo，默认 10m）
    /run                        -- 启动并驱动本项目应用以验证变更；适用于运行、启动、截图或确认应用行为
    /run-skill-generator        -- 编写或改进 run-<unit> skill（项目级 skill，
                                    指导 Agent 如何构建、启动并驱动本项目应用）
    /simplify                   -- 审查已变更代码的复用、简化、效率与抽象层级，并应用修复（仅质量改进，不新增功能）
    /update-config              -- 通过 settings.json 配置 Claude Code；
                                    支持自动化行为（如「以后当 X 时…」「每次 Y 时…」）
    /verify                     -- 运行应用并观察行为，验证代码变更是否达到预期


  更多帮助：https://code.claude.com/docs/en/overview
