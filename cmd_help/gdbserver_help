用法：	gdbserver [选项] COMM PROG [参数 ...]
	gdbserver [选项] --attach COMM PID
	gdbserver [选项] --multi COMM

COMM 可以是 tty 设备（用于串口调试）、
用于监听 TCP 连接的 HOST:PORT，或 '-' / 'stdio' 表示使用 gdbserver 的 stdin/stdout。
PROG 为要调试的可执行程序。ARGS 为传给被调试进程的参数。
使用 --attach 时，PID 为要附加的进程 ID。

运行模式：

  --attach              附加到正在运行的进程 PID。
  --multi               启动服务器时不指定具体程序，
                        仅在收到显式退出命令时才退出。
  --once                在第一次连接关闭后退出。
  --help                打印本说明并退出。
  --version             显示版本信息后退出。

其他选项：

  --wrapper WRAPPER --  通过 WRAPPER 启动新程序。
  --disable-randomization
                        在禁用地址空间随机化的情况下运行 PROG。
  --no-disable-randomization
                        启动 PROG 时不禁用地址空间随机化。
  --startup-with-shell
                        通过 shell 启动 PROG，即先 exec shell，
                        再由 shell exec PROG。（默认）
  --no-startup-with-shell
                        直接 exec PROG，不经过 shell。
                        在类 UNIX 系统上会禁用参数通配符与变量替换。

调试选项：

  --debug               启用一般调试输出。
  --debug-format=OPT1[,OPT2,...]
                        指定调试输出中的额外内容。
                          选项：
                            all
                            none
                            timestamp
  --remote-debug        启用远程协议调试输出。
  --disable-packet=OPT1[,OPT2,...]
                        禁用对 RSP 数据包或特性的支持。
                          选项：
                            vCont、Tthread、qC、qfThreadInfo 以及
                            threads（禁用所有线程相关数据包）。

更多信息请参阅 GDB 手册（在线 info 或纸质版）。
请将问题反馈至 "<http://www.fullhan.com>"。
