
hexdump，hd-以 ASCII、十进制、十六进制或八进制显示文件内容。

格式：
hexdump [options] FILE [...]

选项：
-b       //单字节八进制显示。
-c       //单字节字符显示。
-C       //规范化“十六进制+ASCII”显示。
-d       //两字节十进制显示。
-e <format_string>  //以指定的格式字符串显示数据。
-f <format_file>    //指定包含一个或多个换行分隔格式字符串的文件。内容使用 # 号开头表示注释。
-n <length>         //只解释输入的指定长度个字节。
-o                 //两字节八进制显示。
-s <offset>        //跳过开头指定长度个字节。
-v     //显示时不压缩相似的行。-v 选项使 hextump 显示所有输入数据。没有 -v 选项，任何数量的输出行组，如果与前一组相同，将被替换为由一个星号构成的行。
-x          //两字节十六进制显示。


例子：
跳过1024字节后的1024字节。
$hexdump -C -s 1024 -n 1024  testfile
