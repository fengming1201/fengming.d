用法: rm [选项]... [文件]...
删除（取消链接）指定的文件。

  -f, --force           忽略不存在的文件和参数，从不提示
  -i                    每次删除前都提示
  -I                    删除超过三个文件时，或递归删除时，只提示一次；
                          比 -i 打扰更少，同时仍能防止大多数误操作
      --interactive[=WHEN]  按 WHEN 提示：never（从不）、once（一次，同 -I）或
                          always（总是，同 -i）；省略 WHEN 时等同于总是提示
      --one-file-system  递归删除目录树时，跳过与命令行参数所在
                          文件系统不同的目录
      --no-preserve-root  不把 '/' 当作特殊路径
      --preserve-root[=all]  不删除 '/'（默认）；
                              带 'all' 时，拒绝任何与其父目录不在同一设备上的
                              命令行参数
  -r, -R, --recursive   递归删除目录及其内容
  -d, --dir             删除空目录
  -v, --verbose         显示正在执行的操作
      --help        显示此帮助信息并退出
      --version     显示版本信息并退出

默认情况下，rm 不会删除目录。使用 --recursive（-r 或 -R）
选项可同时删除列出的每个目录及其全部内容。

若尝试删除最后一个路径分量为 '.' 或 '..' 的文件，
将被拒绝并给出诊断信息。

要删除以 '-' 开头的文件名（例如 '-foo'），
可使用以下命令之一：
  rm -- -foo

  rm ./-foo

使用 rm 删除文件后，在具备足够专业知识或时间的情况下，
仍有可能恢复部分内容。若需更高把握确保内容不可恢复，
可考虑使用 shred(1)。

GNU coreutils 在线帮助: <https://www.gnu.org/software/coreutils/>
完整文档 <https://www.gnu.org/software/coreutils/rm>
或本地查看: info '(coreutils) rm invocation'
